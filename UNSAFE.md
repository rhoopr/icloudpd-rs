# Unsafe usage audit

Audit date: 2026-09-01.

This file lists each `unsafe` block or expression in repo-owned production
Rust source. Test modules, `tests/`, plain-text mentions of "unsafe", Cargo
lint settings, and dependency lockfile entries are not counted. There are no
`unsafe fn`, `unsafe impl`, or raw `extern` declarations in production code.

## Production code

| Location | Usage | Justification | Removal assessment |
| --- | --- | --- | --- |
| `src/lib.rs::harden_process` | `libc::prctl(PR_SET_DUMPABLE, 0, 0, 0, 0)` in `harden_process`. | Disables Linux core dumps before secrets can leak into a dump. The call takes integer arguments only, so the local memory-safety risk is limited to correct FFI signature and constants. Failure is logged and ignored. | No safe `std` equivalent. It can be moved out of repo-local `unsafe` by adding a direct `rustix` dependency with the process APIs and calling `rustix::process::set_dumpable_behavior(DumpableBehavior::NotDumpable)`. Dropping the call would weaken credential hardening. |
| `src/lib.rs::harden_process` | `libc::setrlimit(RLIMIT_CORE, &rlim)` in `harden_process`. | Sets the core-file size limit to zero on Unix. The `rlimit` value is stack-allocated, initialized before the call, and the kernel only reads it during the syscall. Failure is logged and ignored. | No safe `std` equivalent. A direct `rustix` process dependency can replace this with `setrlimit(Resource::Core, Rlimit { current: 0, maximum: 0 })`. Dropping it would weaken the same hardening path. |
| `src/lib.rs::available_disk_space` (Unix) | `std::mem::zeroed::<libc::statvfs>()` in `available_disk_space`. | Creates an output buffer for `statvfs`. The struct is integer fields on supported Unix targets, and the following syscall overwrites it on success. | Removable. `fs4` is already a direct dependency and exposes safe `fs4::available_space(path)`, which would remove both this initialization and the raw `statvfs` call below. |
| `src/lib.rs::available_disk_space` (Unix) | `libc::statvfs(c_path.as_ptr(), &raw mut stat)` in `available_disk_space`. | Reads filesystem free-space data through a NUL-terminated path and a valid output pointer that outlives the call. | Removable with the same `fs4::available_space(path).ok()` rewrite as `src/lib.rs::available_disk_space` (Unix). |
| `src/lib.rs::available_disk_space` (Windows) | `GetDiskFreeSpaceExW(...)` in `available_disk_space`. | Reads filesystem free-space data through a NUL-terminated UTF-16 path and a valid output pointer. The API permits null pointers for the unused totals. | Removable with the same `fs4::available_space(path).ok()` rewrite. |
| `src/lib.rs::pid_is_alive` | `libc::kill(pid, 0)` in `pid_is_alive`. | Uses POSIX signal `0` as a process-existence probe. It does not deliver a signal; `EPERM` is treated as alive. | No safe `std` equivalent. A direct `rustix` process dependency can replace it with `test_kill_process`, preserving the `PERM` means alive case. The existing unsafe block is small and well-contained. |
| `src/lib.rs::main_inner` | `std::env::remove_var("ICLOUD_PASSWORD")` in `main_inner`. | Scrubs the password environment variable before the Tokio runtime creates worker threads. This is the narrow window where process environment mutation is safe under Rust's current rules. | Hard to remove while preserving the scrub. Safe `std` has no thread-safe process-env mutation API. Options are to stop scrubbing, move all work into a child process launched with a cleaned environment, or keep a tiny documented unsafe wrapper at startup. |
| `src/download/file.rs::renameat2_confined_blocking` | `libc::syscall(SYS_renameat2, ...)` uses retained parent descriptors with `RENAME_NOREPLACE` or `RENAME_EXCHANGE`. | Both descriptors and names remain live, and callers pass one documented flag.  The kernel cannot redirect publication through a replaced pathname ancestor. | No safe `std` equivalent provides these descriptor-relative Linux rename modes.  A direct `rustix` fs dependency could replace the syscall wrapper. |
| `src/download/file.rs::rename_path_macos` | `renamex_np(...)` performs path-backed `RENAME_EXCL` publication or `RENAME_SWAP` exchange. | Both owned NUL-terminated paths remain live, and callers pass one documented flag. | No safe `std` equivalent provides atomic no-replace rename or exchange on macOS. |
| `src/download/file.rs::rename_confined_macos` | `renameatx_np(...)` performs confined `RENAME_EXCL` publication or `RENAME_SWAP` exchange and rollback. | Both retained directory descriptors and names remain live, and callers pass one documented flag. | No safe `std` equivalent provides descriptor-relative atomic no-replace rename or exchange on macOS. |
| `src/download/file.rs::hard_link_locations` | `libc::linkat(...)` installs a confined verified temporary file under the retained destination parent. | Both retained parent descriptors and NUL-terminated names remain live.  `linkat` atomically refuses an existing destination and never resolves an ancestor pathname again. | No safe `std` API links two names relative to retained directory descriptors.  A direct `rustix` fs dependency could replace this call. |
| `src/download/file.rs::renameat2_blocking` | `libc::syscall(SYS_renameat2, ...)` with `RENAME_NOREPLACE` or `RENAME_EXCHANGE`. | Promotes a verified `.part` file without overwrite, or atomically exchanges it with an explicitly authorized truncated file.  The paths are owned `CString`s and no Rust references are retained by the kernel. | No safe `std` equivalent for either Linux rename mode.  A direct `rustix` fs dependency can replace this with `renameat_with` and matching flags.  Do not rewrite either path as an existence check followed by `rename`; that would reintroduce a race. |
| `src/download/file.rs::move_file_no_replace_blocking` (Windows) | `MoveFileExW(..., MOVEFILE_WRITE_THROUGH)` promotes a `.part` file without overwrite. | Both UTF-16 paths are NUL-terminated and live for the call.  Confined reconciliation callers additionally retain every non-reparse directory ancestor without delete sharing. | Not cleanly removable with `std` while preserving write-through behavior.  `std::fs::rename` would hide this Windows-specific durability choice. |
| `src/download/file.rs::replace_file_with_backup_blocking` (Windows) | `ReplaceFileW(..., REPLACEFILE_WRITE_THROUGH)` atomically replaces an approved file and moves its old bytes to a backup path. | Every UTF-16 path is NUL-terminated and valid for the call.  Confined reconciliation callers retain stable ancestor handles and validate both installed and displaced identities. | No safe `std` equivalent provides atomic replacement with a backup.  Ordinary remove and rename calls would weaken crash and race safety. |
| `src/fs_util.rs::file_identity` (Windows) | `GetFileInformationByHandleEx(..., FileIdInfo, ...)` reads the stable volume serial and 128-bit file identity of an opened file or directory. | The handle remains live and the output pointer references writable `FILE_ID_INFO` storage with the exact supplied size. | A safe Windows handle-information wrapper could return the initialized identity.  Stable `std` does not expose the ReFS-safe 128-bit file ID. |
| `src/fs_util.rs::file_identity` (Windows) | `MaybeUninit::assume_init()` reads the successful `FILE_ID_INFO` result. | The expression runs only after `GetFileInformationByHandleEx` succeeds and initializes the complete structure. | A safe Windows wrapper would return an initialized identity and remove this expression. |
| `src/fs_util.rs::file_identity` (Windows fallback) | `GetFileInformationByHandle(...)` reads the volume serial and 64-bit file index when `FileIdInfo` is unavailable or zero. | The handle remains live and the output pointer references writable storage of the exact structure Windows initializes.  A zero fallback index is rejected. | A safe Windows wrapper could return the initialized fallback identity.  Stable `std` does not expose it. |
| `src/fs_util.rs::file_identity` (Windows fallback) | `MaybeUninit::assume_init()` reads the successful fallback handle-information result. | The expression runs only after `GetFileInformationByHandle` succeeds and initializes the complete structure. | A safe Windows wrapper would return an initialized identity and remove this expression. |
| `src/fs_util.rs::openat_owned` | `openat(...)` opens or exclusively creates a file or directory relative to a retained directory descriptor. | The descriptor and NUL-terminated name remain live, callers provide the complete flags and mode, the mode is promoted to `c_uint` for the variadic ABI, and a successful descriptor is newly owned. | No safe `std` API opens relative to an existing directory descriptor.  A direct `rustix` fs dependency could replace the call. |
| `src/fs_util.rs::openat_owned` | `OwnedFd::from_raw_fd(...)` takes ownership of the successful `openat` result. | The descriptor is nonnegative, newly returned by the kernel, and transferred exactly once. | A safe descriptor wrapper would remove this conversion. |
| `src/fs_util.rs::ConfinedPath::entry_exists` (Unix) | `fstatat(..., AT_SYMLINK_NOFOLLOW)` inspects the exact final directory entry. | The retained parent descriptor, NUL-terminated name, and output storage remain valid for the call.  The result is used only as an existence decision, so uninitialized output is never read. | No safe `std` API inspects a name relative to an open directory without following links. |
| `src/fs_util.rs::ConfinedPath::remove_through_private_quarantine` (Unix) | `mkdirat(...)` creates a random mode-0700 child quarantine beneath the retained parent. | The parent descriptor and NUL-terminated random name remain live.  Exclusive directory creation establishes the private namespace used for the later name-bound unlink. | No safe `std` API creates a directory relative to a retained descriptor. |
| `src/fs_util.rs::ConfinedPath::remove_through_private_quarantine` (Unix) | `renameat(...)` moves the current leaf into the empty private quarantine. | Both retained directory descriptors and names remain live.  The move removes the mutable public leaf before identity verification and any unlink. | No safe `std` API renames relative to two retained descriptors. |
| `src/fs_util.rs::ConfinedPath::remove_through_private_quarantine` (Unix cleanup) | `unlinkat(..., AT_REMOVEDIR)` removes an empty quarantine after a failed move. | The retained parent descriptor and quarantine name remain live, and the directory is still empty because the move failed. | No safe `std` API removes a directory relative to a retained descriptor. |
| `src/fs_util.rs::ConfinedPath::remove_through_private_quarantine` (Unix file) | `unlinkat(...)` removes the verified entry from the private quarantine. | The private directory descriptor and fixed name remain live.  No public mutable leaf exists between verification and unlink; ambiguous entries are retained instead. | No safe `std` API removes a name relative to a retained descriptor. |
| `src/fs_util.rs::ConfinedPath::remove_through_private_quarantine` (Unix directory) | `unlinkat(..., AT_REMOVEDIR)` removes the now-empty private quarantine. | The retained parent descriptor and quarantine name remain live, and the verified file has already been removed. | No safe `std` API removes a directory relative to a retained descriptor. |
| `src/fs_util.rs::ConfinedPath::open_platform` (Unix) | `mkdirat(...)` creates a missing descendant directory relative to its retained parent. | The parent descriptor and component remain live.  A competing entry is reopened with `O_NOFOLLOW` and accepted only if it is a real directory. | No safe `std` API creates relative to an open directory descriptor. |
| `src/fs_util.rs::remove_open_windows_file` | `SetFileInformationByHandle(..., FileDispositionInfo, ...)` marks the verified file handle for deletion. | The handle was opened with delete access and remains live.  The disposition value has the required layout and size, so deletion stays bound to that file. | No safe `std` API deletes by an existing file handle.  Path-based removal would restore the race. |
| `src/fs_util.rs::windows_file_attributes` | `GetFileInformationByHandle(...)` reads directory, regular-file, and reparse attributes from a retained handle. | The handle remains live and the output pointer references storage with the exact Windows structure layout. | A safe Windows wrapper could return initialized handle information.  Stable `std` does not expose the required reparse attribute. |
| `src/fs_util.rs::windows_file_attributes` | `MaybeUninit::assume_init()` reads the successful handle-information result. | The expression runs only after `GetFileInformationByHandle` succeeds and initializes the complete structure. | A safe Windows wrapper would return an initialized value and remove this expression. |
| `src/download/metadata.rs::exif_datetime_to_iso` | `String::as_bytes_mut()` in `exif_datetime_to_iso`. | Mutates three delimiter bytes in an owned `String`. The length and delimiter checks prove the byte positions exist, and the replacement bytes are ASCII, so UTF-8 stays valid. | Removable. Build a `Vec<u8>` from `s.as_bytes()`, mutate the vector with safe indexing after the same length checks, and convert with `String::from_utf8`. A formatting-based rewrite would also work. |
| `src/personality/tty_echo.rs::EchoGuard::install` | `std::mem::zeroed::<termios>()` before `tcgetattr` in `EchoGuard::install`. | Prepares a `termios` output buffer. `tcgetattr` fills it before fields are read. | Removable by switching the module to safe termios bindings, for example a direct `rustix` dependency with the `termios` feature. That would also remove the related `tcgetattr` and `tcsetattr` unsafe calls. |
| `src/personality/tty_echo.rs::EchoGuard::install` | `tcgetattr(STDIN_FILENO, &mut t)` in `EchoGuard::install`. | Reads terminal flags from stdin into a valid `termios` pointer. Failure returns `None`. | Removable with safe termios bindings such as `rustix::termios::tcgetattr(std::io::stdin())`. |
| `src/personality/tty_echo.rs::EchoGuard::install` | `tcsetattr(STDIN_FILENO, TCSANOW, &t)` in `EchoGuard::install`. | Writes the modified terminal flags back to stdin. The input pointer is valid for the call. | Removable with safe termios bindings such as `rustix::termios::tcsetattr`. |
| `src/personality/tty_echo.rs::EchoGuard::restore_now` | `std::mem::zeroed::<termios>()` before restore-time `tcgetattr`. | Prepares a `termios` output buffer during terminal restore. | Removable with the same safe termios rewrite as `src/personality/tty_echo.rs::EchoGuard::install`. |
| `src/personality/tty_echo.rs::EchoGuard::restore_now` | `tcgetattr(STDIN_FILENO, &mut t)` in `restore_now`. | Reads the current terminal flags so only the saved local flags are restored. Failure is ignored because shutdown recovery is best-effort. | Removable with the same safe termios rewrite as `src/personality/tty_echo.rs::EchoGuard::install`. |
| `src/personality/tty_echo.rs::EchoGuard::restore_now` | `tcsetattr(STDIN_FILENO, TCSANOW, &t)` in `restore_now`. | Restores the saved terminal flags. Failure is ignored because the next shell prompt normally resets tty state. | Removable with the same safe termios rewrite as `src/personality/tty_echo.rs::EchoGuard::install`. |
| `src/service/env.rs::effective_uid` | `libc::geteuid()` in `effective_uid`. | Centralizes effective-UID lookup for Unix service backends. `geteuid` is stateless and has no pointer or aliasing preconditions. | Removable by adding a direct safe wrapper dependency, for example `rustix::process::geteuid` or `nix::unistd::geteuid`. This is a good cleanup target because tests duplicate this same unsafe call. |

## Best production removal candidates

The easiest local removals are:

1. Replace `available_disk_space` with `fs4::available_space`, removing
   both Unix expressions and the Windows FFI call.
2. Rewrite `exif_datetime_to_iso` without `String::as_bytes_mut`, removing
   its local `unsafe` block.
3. Add a direct `rustix` dependency for process, fs, and termios wrappers. That
   would remove most Unix syscall wrappers while preserving their semantics.
